import { OroltsooAdminProfile } from '@/profile/graphql/resolvers/customResolvers/profile';

const { fullName, promiseProgress } = OroltsooAdminProfile;

export const CpOroltsooProfile = {
  fullName,
  promiseProgress,
};
